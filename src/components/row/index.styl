@import '../styles/vars'
@import '../styles/mixins'

$prefixCls = berry-row

.{$prefixCls}
    display: flex
    flex-wrap: wrap

    &.align-left
        justify-content: flex-start

    &.align-right
        justify-content: flex-end

    &.align-center
        justify-content: center
